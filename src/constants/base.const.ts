/**
 * Base constants for the library.
 *
 * Centralized place for network-related constants so other modules
 * (e.g. HTTP configuration) can import them from here.
 */

export const BASE_URL = "https://api.jikan.moe/v4" as const;
